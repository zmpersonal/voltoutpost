
async function loadIndex(){const r=await fetch('/data/resilience-index.json');return r.json()}
function esc(s){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
async function initLookup(){const input=document.querySelector('#cityLookup');const box=document.querySelector('#lookupResults');if(!input||!box)return;const d=await loadIndex();input.addEventListener('input',()=>{let q=input.value.toLowerCase().trim();if(q.length<2){box.innerHTML='';return}let m=d.cities.filter(x=>(x.city+' '+x.state).toLowerCase().includes(q)).slice(0,8);box.innerHTML=m.map(x=>`<a class="rankrow" href="/cities/${x.slug}/"><span class="ranknum">#${x.rank}</span><span><b>${esc(x.city)}, ${x.state_abbr}</b><br><small>${esc(x.county)}</small></span><span class="scorepill">${x.score}</span></a>`).join('')})}
function initCalc(){let form=document.querySelector('#runtimeCalc');if(!form)return;function go(){let cap=+document.querySelector('#battery').value||0, reserve=+document.querySelector('#reserve').value||10, load=+document.querySelector('#load').value||0;let usable=cap*(1-reserve/100), hrs=load>0?usable/load:0;document.querySelector('#runtime').textContent=hrs.toFixed(1)+' hrs';document.querySelector('#usable').textContent=usable.toFixed(1)+' kWh';}form.addEventListener('input',go);go()}
document.addEventListener('DOMContentLoaded',()=>{initLookup();initCalc()});
